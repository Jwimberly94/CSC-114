Before and after comparisons.

Since Check-In 1, I changed my project from a single script to a modular setup and added a full GUI 
with multiple plots.

My before-and-after comparison is:

My Test MAE changed from 0.597 to 0.631 (x $100,000), so in this run my model error got a 
little worse instead of better.


My project output improved from 1 graph to 4 graphs. Before, I only had a training history plot.
Now, I also have:
actual vs predicted trend,
county price comparison,
estimated utilities by county.

I also improved usability by moving from a script-only workflow to a GUI where I can run the model, 
view logs, and switch between chart tabs.

So overall, my model accuracy did not improve in this run, but my analysis and presentation improved a lot.
